# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/

# Swift
.build/
Package.resolved
.swiftpm/

# macOS
.DS_Store

# XcodeGen
*.xcodeproj
apple/App/Info.plist

# Claude Code: .claude/ is committed (shared settings, skills). Personal overrides stay local.
.claude/settings.local.json

# Superpowers brainstorm sessions (visual companion mockups)
.superpowers/

# Rust build artifacts
target/

# mdBook output (docs/book/src is the source; the site is built by CI)
docs/book/book/

# Local test fixtures (large video files; not tracked)
fixtures/

# Git worktrees for feature development
.worktrees/

# Per-session git worktrees created by Claude Code; transient, never committed.
.claude/worktrees/
